///|
/// Version text, mirroring the reference CLI's `ArturoVersionText`
/// (`src/vm/version.nim`). The help screen is rendered from the
/// argparse command specification in `cli.mbt` instead.

///|
fn version_text() -> String {
  "arturo 0.1.0 (MoonBit port)"
}
